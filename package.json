{
  "name": "salescoach-ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Powered Sales Coach for Commercial Calls",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "db:migrate": "cd apps/api && alembic upgrade head",
    "db:seed": "cd apps/api && python -m app.utils.seed"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@10.34.5"
}
